import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  // Reuse legacy repository audio samples without duplicating files.
  publicDir: '../sounds',
})
